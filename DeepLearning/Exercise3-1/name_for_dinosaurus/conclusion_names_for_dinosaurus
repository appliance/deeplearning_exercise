总结基于基础的RNN构建字符级别的姓名预测模型
模型整体构建步骤
    initialize_parameters()  权重参数初始化
        其中激活值矩阵的行数 n_a 应该是可以随机调制的（大小不受什么东西限制）

    rnn_forward() 前向计算
        注意的是 传入的X 和 Y 是否经过向量化处理，还是传入的就是原始的文本转下标的值
        模型计算的公式 需要记清楚并理解

    rnn_backward() 反向计算
        有点难，算不出来，直接抄袭别人的公式

    updata_parameters() 参数更新

    clip() 梯度裁剪 模型优化处理

    optimize() 将以上 一此 从前向计算(包含损失函数计算)、反向计算、参数更新 进行整合

    model() 设计迭代此处 循环执行optimize()

    sample() 利用已经训练好的模型参数 进行取样预测 观看预测效果